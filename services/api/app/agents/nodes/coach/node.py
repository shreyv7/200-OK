from __future__ import annotations

from typing import Any


def coach_node(state: dict[str, Any]) -> dict[str, Any]:
  """P2 stub — registered for graph completeness only."""
  return {"visited": ["coach"]}
