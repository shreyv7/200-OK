# Clerk (required for A4 — copy publishable key from Clerk dashboard)
VITE_CLERK_PUBLISHABLE_KEY=

# Backend API (local default matches docker/API on :8002)
# Leave unset to use same-origin /api/v1 with Vite proxy → localhost:8002
# VITE_API_BASE=http://localhost:8002/api/v1
